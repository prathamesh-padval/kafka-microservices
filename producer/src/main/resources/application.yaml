server:
  port: 8081

spring:
  application:
    name: producer
  kafka:
    topic: test
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}
    acks: all
    retries: 5
    batch-size: 32768
    linger-ms: 10
    compression-type: none

